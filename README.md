# RoboCode Homebrew Tap

Install RoboCode with Homebrew:

```bash
brew tap wikieden/tap
brew install robocode
```

Or install in one command:

```bash
brew install wikieden/tap/robocode
```

Project repository: https://github.com/wikieden/robocode
